{
  "name": "fengcing-electron",
  "version": "1.0.0",
  "description": "梅峰小学击剑问答一体机",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "package": "electron-packager . digitalTwin --platform=win32 --arch=x64 --icon=assets/app-icon.ico --out=./out --asar --app-version=1.0.0 --overwrite --ignore=node_modules"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^21.0.1",
    "electron-packager": "^16.0.0"
  },
  "permissions": [
    "camera"
  ]
}
